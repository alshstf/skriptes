# Тестовые данные

Сюда положите небольшой `.inpx` (например, 3 inp-файла с 50 книгами) для unit/integration тестов парсера и импортёра.

Этот файл попадёт в repo, поэтому он должен быть маленьким (< 1 MB) и не содержать чувствительных метаданных.
